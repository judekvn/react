const bookDefaultState = {
    book: null,
    books: null,
    loading: false
}

export default (state = bookDefaultState, action) => {
    switch (action.type) {
        default:
            return state
    }
}